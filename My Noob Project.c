#include <stdio.h>
int main(){
    int x, y;
    float z;
    char m;
    double bip;
    scanf("%d %f %c %lf", &x, &z, &m, &bip);
    y=z+x+5;
printf("hey Prapty\n How are you?\t\n How is your RAPL session going?\n Output %d and float %f char %c lol %lf", y,z,m, bip);
return 0;
}

